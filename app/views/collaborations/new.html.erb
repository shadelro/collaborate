<p>Create a Collaboration:</p>
<%= form_for [current_user, @collaboration] do |f| %>
  <div class='fields'>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit %>
<% end %>
